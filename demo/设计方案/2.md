# 萌宠网站高级设计方案 v3.0

## 项目概述

**项目名称**：萌宠星球  
**文件路径**：`page-demo/demo7/pets.html`  
**核心功能**：萌宠展示、宠物服务、养宠知识、故事分享、联系领养  
**技术栈**：HTML + CSS + JavaScript + GSAP + ScrollTrigger

---

## 一、页面结构

### 1.1 整体布局

```
┌─────────────────────────────────────────────┐
│              导航栏 (Navbar)                │
│         + 滚动进度指示器                    │
├─────────────────────────────────────────────┤
│              Hero 首屏区域                   │
│    Canvas粒子动画 + 标题打字效果 + 呼吸动画   │
├─────────────────────────────────────────────┤
│           横向滚动区域 (4个面板)             │
│  ┌──────┬──────┬──────┬──────┐              │
│  │面板1 │面板2 │面板3 │面板4 │              │
│  │相册  │展示  │服务  │知识  │              │
│  └──────┴──────┴──────┴──────┘              │
│         + 右侧导航点 + 进度条                 │
├─────────────────────────────────────────────┤
│             纵向滚动区域                     │
│  ┌─────────┬─────────┬─────────┐            │
│  │数据统计 │宠物故事 │联系我们 │            │
│  │数字递增 │旋转卡片 │表单验证 │            │
│  └─────────┴─────────┴─────────┘            │
├─────────────────────────────────────────────┤
│              页脚 (Footer)                  │
│         + 返回顶部按钮                       │
└─────────────────────────────────────────────┘
```

### 1.2 面板详细说明

| 面板 | 类名 | 内容 | 卡片类型 | 卡片数量 | 特色动画 |
|------|------|------|----------|----------|----------|
| 面板1 | `.panel-gallery` | 萌宠相册 | `.gallery-item` | 8个 | 水墨晕染揭示、图片缩放 |
| 面板2 | `.panel-pets` | 宠物展示 | `.pet-card` | 6个 | 3D翻转浮动、图标旋转 |
| 面板3 | `.panel-services` | 服务项目 | `.service-card` | 6个 | 毛玻璃卡片、边框发光 |
| 面板4 | `.panel-knowledge` | 养宠知识 | `.knowledge-card` | 4个 | 视差滚动、文字渐显 |

---

## 二、设计规范

### 2.1 色彩系统

```css
/* 主色调 */
--primary: #ff6b81;        /* 主题粉红 */
--primary-dark: #ff4757;   /* 深粉红 */
--primary-light: #ff8fa3;  /* 浅粉红 */

/* 渐变背景 */
--gradient-pink: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
--gradient-purple: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
--gradient-orange: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
--gradient-blue: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
--gradient-green: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
--gradient-gold: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
--gradient-sunset: linear-gradient(135deg, #ff6b6b, #feca57, #48dbfb);
--gradient-ocean: linear-gradient(135deg, #0f2027, #203a43, #2c5364);

/* 文字颜色 */
--text-dark: #333;
--text-light: #fff;
--text-muted: #666;
--text-gray: #999;

/* 卡片渐变预设 (用于avatar/icon背景) */
gradient-1: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);  /* 橙黄 */
gradient-2: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%);  /* 浅蓝 */
gradient-3: linear-gradient(135deg, #d4fc79 0%, #96e6a1 100%);  /* 绿 */
gradient-4: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);  /* 粉 */
gradient-5: linear-gradient(135deg, #a18cd1 0%, #fbc2eb 100%);  /* 紫 */
gradient-6: linear-gradient(135deg, #fad0c4 0%, #ffd1ff 100%);  /* 粉紫 */
gradient-7: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%);  /* 青绿 */
gradient-8: linear-gradient(135deg, #fccb90 0%, #d57eeb 100%);  /* 金紫 */
```

### 2.2 字体规范

```css
/* 字体族 */
font-family: 'Segoe UI', 'Microsoft YaHei', sans-serif;

/* 字号系统 */
--font-hero: clamp(48px, 8vw, 96px);    /* Hero标题 */
--font-title: clamp(32px, 5vw, 48px);   /* 区块标题 */
--font-heading: 24px;                   /* 卡片标题 */
--font-body: 14px;                      /* 正文 */
--font-small: 12px;                    /* 辅助文字 */

/* 字重 */
font-weight: 300;   /* 细体 */
font-weight: 400;   /* 常规 */
font-weight: 600;   /* 中等 */
font-weight: 700;   /* 加粗 */
font-weight: 800;   /* 特粗 */
font-weight: 900;   /* 黑体 */

/* 文字阴影效果 */
text-shadow: 0 0 30px rgba(255, 107, 129, 0.5);  /* 发光效果 */
text-shadow: 0 2px 0 #d64a6a;  /* 立体效果 */

/* 渐变文字 */
background: linear-gradient(135deg, #ff6b81, #ff4757);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
```

### 2.3 间距规范

```css
/* 面板内边距 */
.scroll-panel {
  padding: 80px;
}

/* 卡片内边距 */
.pet-card { padding: 30px; }
.service-card { padding: 25px; }
.gallery-item { padding: 30px 20px; }

/* 网格间距 */
--gap-large: 40px;
--gap-medium: 30px;
--gap-small: 15px;

/* 圆角系统 */
--radius-small: 10px;
--radius-medium: 20px;
--radius-large: 25px;
--radius-full: 50%;
```

### 2.4 阴影系统

```css
/* 卡片阴影 */
box-shadow: 0 20px 50px rgba(0, 0, 0, 0.1);

/* 悬停阴影 */
box-shadow: 0 30px 60px rgba(0, 0, 0, 0.15);

/* 发光阴影 */
box-shadow: 0 0 30px rgba(255, 107, 129, 0.4);
box-shadow: 0 0 60px rgba(255, 107, 129, 0.2);

/* 毛玻璃效果 */
backdrop-filter: blur(20px);
-webkit-backdrop-filter: blur(20px);

/* 边框发光脉冲 */
@keyframes glow-pulse {
  0%, 100% { box-shadow: 0 0 10px rgba(100, 200, 255, 0.3); }
  50% { box-shadow: 0 0 30px rgba(100, 200, 255, 0.6); }
}

/* 3D阴影 */
box-shadow: 
  0 2px 0 #d64a6a,  /* 底部高光 */
  0 4px 0 #c43d5a,   /* 中间阴影 */
  0 6px 0 #b2304a,   /* 顶部阴影 */
  0 8px 20px rgba(255, 71, 87, 0.4);  /* 底部发光 */
```

---

## 三、组件设计

### 3.1 卡片组件标准结构

```html
<!-- 标准卡片模板 -->
<div class="card-class">
  <!-- 背景层（可选） -->
  <div class="card-bg"></div>
  
  <!-- 内容层 -->
  <div class="card-content">
    <!-- 头部：头像/图标 -->
    <div class="card-avatar" style="background: linear-gradient(...)">
      内容或图标
    </div>
    
    <!-- 编号（可选） -->
    <div class="card-number">01</div>
    
    <!-- 标题 -->
    <h3 class="card-name">名称</h3>
    
    <!-- 副标题 -->
    <p class="card-breed">类型</p>
    
    <!-- 描述 -->
    <p class="card-desc">描述文字</p>
    
    <!-- 进度条（可选） -->
    <div class="card-progress">
      <div class="progress-bar"></div>
    </div>
    
    <!-- 标签组 -->
    <div class="card-tags">
      <span class="card-tag">标签1</span>
      <span class="card-tag">标签2</span>
    </div>
  </div>
  
  <!-- 边框层（毛玻璃卡片用） -->
  <div class="card-border"></div>
  
  <!-- 高光层（鼠标跟随效果） -->
  <div class="card-highlight"></div>
</div>
```

### 3.2 卡片CSS标准

```css
/* 卡片基础样式 */
.card-class {
  position: relative;
  background: rgba(255, 255, 255, 0.95);
  border-radius: 25px;
  padding: 30px;
  text-align: center;
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.1);
  opacity: 1 !important;
  transition: all 0.3s ease;
  overflow: hidden;
  cursor: pointer;
}

/* 悬停效果 - 多层变换 */
.card-class:hover {
  transform: translateY(-10px) scale(1.02);
  box-shadow: 0 30px 60px rgba(167, 139, 250, 0.4);
}

/* 毛玻璃卡片 */
.glass-card {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

/* 边框发光卡片 */
.border-glow-card {
  position: relative;
}

.border-glow-card::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  border: 2px solid rgba(100, 200, 255, 0.3);
  animation: glow-pulse 3s ease-in-out infinite;
}
```

### 3.3 头像/图标组件

```css
/* 圆形头像（用于萌宠展示） */
.avatar {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  margin: 0 auto 15px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 50px;
  transition: all 0.3s ease;
}

/* 头像悬停放大 */
.avatar:hover {
  transform: scale(1.1) rotate(5deg);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
}

/* 方形图标（用于服务展示） */
.icon {
  height: 150px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 80px;
}

/* 图标发光效果 */
.icon {
  filter: drop-shadow(0 0 20px rgba(100, 200, 255, 0.6));
}
```

### 3.4 标签组件

```css
/* 标签组 */
.tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  justify-content: center;
}

/* 单个标签 */
.tag {
  padding: 4px 12px;
  background: linear-gradient(135deg, #ff6b81 0%, #ff4757 100%);
  border-radius: 15px;
  font-size: 11px;
  color: #fff;
  transition: all 0.3s ease;
}

/* 标签悬停 */
.tag:hover {
  transform: scale(1.1);
  box-shadow: 0 5px 15px rgba(255, 107, 129, 0.4);
}
```

---

## 四、高级动画技巧（GSAP核心）

### 4.0 GSAP时间线动画（Timeline）

```javascript
// 时间线动画 - 按顺序执行多个动画
const masterTimeline = gsap.timeline()

// 阶段1：背景动画
masterTimeline
  .to('.bg-shape-1', { scale: 1.5, rotation: 180, duration: 2, ease: 'power2.out' })
  .to('.bg-shape-2', { scale: 1.3, rotation: -90, duration: 1.5, ease: 'power2.out' }, '-=1')
  .to('.bg-particles', { opacity: 0.8, duration: 1 }, '-=0.5')

// 阶段2：标题动画
masterTimeline
  .from('.hero-title', { y: 100, opacity: 0, duration: 1, ease: 'power3.out' })
  .from('.hero-subtitle', { y: 50, opacity: 0, duration: 0.8, ease: 'power2.out' }, '-=0.5')
  .from('.hero-cta', { scale: 0, duration: 0.5, ease: 'back.out(1.7)' }, '-=0.3')

// 阶段3：装饰动画
masterTimeline
  .from('.decoration', { rotation: -180, scale: 0, stagger: 0.1, duration: 0.6, ease: 'back.out(1.7)' })
  .to('.scroll-indicator', { opacity: 1, duration: 0.5 }, '-=0.2')
```

### 4.1 SVG路径绘制动画（Path Draw）

```javascript
// SVG路径绘制动画
gsap.to('.svg-path', {
  strokeDashoffset: 0,
  duration: 2,
  ease: 'power2.inOut'
})

// 多段路径依次绘制
const paths = gsap.utils.toArray('.multi-path')
paths.forEach((path, i) => {
  const length = path.getTotalLength()
  gsap.set(path, { strokeDasharray: length, strokeDashoffset: length })
  
  gsap.to(path, {
    strokeDashoffset: 0,
    duration: 1.5,
    delay: i * 0.3,
    ease: 'power2.inOut'
  })
})

// 绘制完成后发光效果
gsap.to('.svg-path', {
  filter: 'drop-shadow(0 0 20px rgba(100, 200, 255, 0.8))',
  duration: 0.5,
  delay: 2
})
```

### 4.2 文字拆解动画（SplitText）

```javascript
// 文字逐字动画
const chars = element.textContent.split('')
element.innerHTML = chars.map(char => 
  `<span class="char" style="display:inline-block">${char}</span>`
).join('')

gsap.from('.char', {
  y: 100,
  opacity: 0,
  rotationX: -90,
  stagger: 0.05,
  duration: 0.6,
  ease: 'back.out(1.7)'
})

// 波浪效果
gsap.to('.char', {
  y: (i) => Math.sin(i * 0.5) * 20,
  duration: 1,
  repeat: -1,
  yoyo: true,
  ease: 'sine.inOut',
  stagger: 0.1
})
```

### 4.3 3D透视卡片动画（3D Transform）

```javascript
// 3D卡片翻转效果
const flipCard = document.querySelector('.flip-card')
const flipInner = flipCard.querySelector('.flip-inner')

// 3D翻转动画
gsap.to(flipInner, {
  rotateY: 180,
  duration: 1,
  ease: 'power2.inOut'
})

// 鼠标控制的3D倾斜
const tiltCard = (event, card) => {
  const rect = card.getBoundingClientRect()
  const x = event.clientX - rect.left
  const y = event.clientY - rect.top
  const centerX = rect.width / 2
  const centerY = rect.height / 2
  
  // 计算旋转角度
  const rotateX = (y - centerY) / 10
  const rotateY = (centerX - x) / 10
  
  gsap.to(card, {
    rotateX: rotateX,
    rotateY: rotateY,
    transformPerspective: 1000,
    duration: 0.3,
    ease: 'power2.out'
  })
}

// 多层3D堆叠效果
gsap.utils.toArray('.stack-card').forEach((card, i) => {
  gsap.to(card, {
    z: i * 30,
    rotateZ: (i - 2) * 5,
    scrollTrigger: {
      trigger: '.stack-section',
      start: 'top center',
      end: 'bottom center',
      scrub: 1
    }
  })
})
```

### 4.4 遮罩揭示动画（Mask Reveal）

```javascript
// 圆形揭示动画
gsap.fromTo('.reveal-mask',
  { clipPath: 'circle(0% at 50% 50%)' },
  {
    clipPath: 'circle(150% at 50% 50%)',
    duration: 1.5,
    ease: 'power2.inOut'
  }
)

// 多边形揭示
gsap.fromTo('.reveal-poly',
  { clipPath: 'polygon(50% 50%, 50% 50%, 50% 50%, 50% 50%)' },
  {
    clipPath: 'polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%)',
    duration: 1.2,
    ease: 'expo.inOut'
  }
)

// 波浪揭示
gsap.fromTo('.reveal-wave',
  { clipPath: 'polygon(0% 100%, 0% 100%, 0% 100%, 0% 100%)' },
  {
    clipPath: 'polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%)',
    duration: 1,
    ease: 'power2.out'
  }
)
```

### 4.5 序列入场动画（Stagger Sequence）

```javascript
// 卡片序列入场 - 瀑布效果
gsap.from('.stagger-card', {
  y: 80,
  opacity: 0,
  scale: 0.9,
  stagger: {
    amount: 1,      // 总时长
    from: 'random'  // 随机顺序
  },
  duration: 0.8,
  ease: 'power2.out'
})

// 波浪入场
gsap.from('.wave-item', {
  y: 50,
  opacity: 0,
  scale: 0.8,
  stagger: {
    each: 0.1,
    from: 'center',  // 从中心向两边
    grid: 'auto'
  },
  duration: 0.6,
  ease: 'back.out(1.7)'
})

// 螺旋入场
gsap.from('.spiral-item', {
  rotation: 720,
  scale: 0,
  stagger: 0.15,
  duration: 1,
  ease: 'elastic.out(1, 0.5)'
})
```

### 4.6 拖拽交互动画（Draggable）

```javascript
// 拖拽卡片
Draggable.create('.drag-card', {
  type: 'x,y',
  bounds: '.container',
  inertia: true,
  
  onDragStart: function() {
    gsap.to(this.target, { scale: 1.1, zIndex: 100 })
  },
  
  onDragEnd: function() {
    gsap.to(this.target, { 
      scale: 1, 
      zIndex: 1,
      ease: 'elastic.out(1, 0.5)'
    })
  }
})

// 拖拽后弹回
const snapBack = (dragged) => {
  gsap.to(dragged, {
    x: 0,
    y: 0,
    rotation: 0,
    duration: 0.5,
    ease: 'elastic.out(1, 0.5)'
  })
}
```

### 4.7 数值动画（MorphSVG/Dynamic Values）

```javascript
// 数字递增动画
gsap.to('.stat-number', {
  textContent: 1000,
  duration: 2,
  ease: 'power1.out',
  snap: { textContent: 1 },
  onUpdate: function() {
    const value = Math.round(this.targets()[0].textContent)
    this.targets()[0].textContent = value.toLocaleString()
  }
})

// SVG图形变形
const morphTl = gsap.timeline()
morphTl
  .to('.morph-shape', {
    morphSVG: '.target-shape',
    duration: 1,
    ease: 'power2.inOut'
  })
  .to('.morph-shape', {
    morphSVG: '.final-shape',
    duration: 1,
    ease: 'power2.inOut'
  })

// 进度环动画
gsap.to('.progress-ring-circle', {
  strokeDashoffset: 0,
  duration: 2,
  ease: 'power2.out'
})
```

### 4.8 粒子动画系统（Particle System）

```javascript
// Canvas粒子系统
class ParticleSystem {
  constructor(canvas) {
    this.canvas = canvas
    this.ctx = canvas.getContext('2d')
    this.particles = []
    this.init()
  }

  createParticle() {
    return {
      x: Math.random() * this.canvas.width,
      y: Math.random() * this.canvas.height,
      size: Math.random() * 5 + 1,
      speedX: (Math.random() - 0.5) * 2,
      speedY: (Math.random() - 0.5) * 2,
      hue: Math.random() * 60 + 330, // 粉色范围
      opacity: Math.random() * 0.5 + 0.5
    }
  }

  update() {
    this.particles.forEach(p => {
      p.x += p.speedX
      p.y += p.speedY
      
      // 边界反弹
      if (p.x < 0 || p.x > this.canvas.width) p.speedX *= -1
      if (p.y < 0 || p.y > this.canvas.height) p.speedY *= -1
    })
  }

  draw() {
    this.ctx.clearRect(0, 0, this.canvas.width, this.canvas.height)
    
    this.particles.forEach(p => {
      this.ctx.beginPath()
      this.ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2)
      this.ctx.fillStyle = `hsla(${p.hue}, 100%, 60%, ${p.opacity})`
      this.ctx.fill()
    })
    
    // 绘制连线
    this.connectParticles()
  }

  connectParticles() {
    for (let i = 0; i < this.particles.length; i++) {
      for (let j = i + 1; j < this.particles.length; j++) {
        const dx = this.particles[i].x - this.particles[j].x
        const dy = this.particles[i].y - this.particles[j].y
        const distance = Math.sqrt(dx * dx + dy * dy)
        
        if (distance < 150) {
          this.ctx.beginPath()
          this.ctx.moveTo(this.particles[i].x, this.particles[i].y)
          this.ctx.lineTo(this.particles[j].x, this.particles[j].y)
          this.ctx.strokeStyle = `rgba(255, 107, 129, ${1 - distance/150})`
          this.ctx.stroke()
        }
      }
    }
  }

  animate() {
    this.update()
    this.draw()
    requestAnimationFrame(() => this.animate())
  }
}

// 启动粒子系统
const particles = new ParticleSystem(document.getElementById('particle-canvas'))
particles.animate()
```

### 4.9 滚动同步动画（Scroll Sync）

```javascript
// 多元素滚动同步
const syncElements = gsap.utils.toArray('.sync-item')

syncElements.forEach((el, i) => {
  gsap.to(el, {
    x: (i % 2 === 0) ? -100 : 100,
    scrollTrigger: {
      trigger: '.sync-section',
      start: 'top center',
      end: 'bottom center',
      scrub: {
        smoothChildTiming: true
      }
    }
  })
})

// 进度指示器同步
gsap.to('.progress-indicator', {
  scaleX: ScrollTrigger.progress,
  ease: 'none',
  scrollTrigger: {
    trigger: '.progress-section',
    start: 'top top',
    end: 'bottom bottom',
    scrub: true
  }
})

// 背景颜色随滚动变化
gsap.to('.color-scroll-bg', {
  backgroundColor: '#ff6b81',
  scrollTrigger: {
    trigger: '.color-section',
    start: 'top center',
    end: 'bottom center',
    scrub: 1
  }
})
```

### 4.10 复杂过渡动画（Complex Transitions）

```javascript
// 页面切换过渡
const pageTransition = gsap.timeline()

pageTransition
  .to('.current-page', {
    x: '-100%',
    opacity: 0,
    scale: 0.8,
    duration: 0.5,
    ease: 'power2.inOut'
  })
  .from('.next-page', {
    x: '100%',
    opacity: 0,
    scale: 1.2,
    duration: 0.5,
    ease: 'power2.inOut'
  }, '-=0.2')

// 模态框动画
const openModal = () => {
  gsap.timeline()
    .to('.modal-backdrop', { opacity: 1, duration: 0.3 })
    .fromTo('.modal-content',
      { scale: 0, rotation: -15, opacity: 0 },
      { scale: 1, rotation: 0, opacity: 1, duration: 0.5, ease: 'back.out(1.7)' }
    )
    .from('.modal-item', {
      y: 30,
      opacity: 0,
      stagger: 0.1,
      duration: 0.4,
      ease: 'power2.out'
    }, '-=0.2')
}

const closeModal = () => {
  gsap.timeline()
    .to('.modal-content', { scale: 0, rotation: 15, opacity: 0, duration: 0.3 })
    .to('.modal-backdrop', { opacity: 0, duration: 0.2 }, '-=0.1')
}
```

### 4.11 字符打字机效果（Typewriter）

```javascript
// 打字机效果
const typewriter = (element, text, speed = 50) => {
  let i = 0
  element.textContent = ''
  
  const type = () => {
    if (i < text.length) {
      element.textContent += text.charAt(i)
      i++
      setTimeout(type, speed)
    }
  }
  
  type()
}

// 光标闪烁
gsap.to('.cursor', {
  opacity: 0,
  duration: 0.5,
  repeat: -1,
  yoyo: true,
  ease: 'steps(1)'
})

// 删除效果
const deleteText = (element, speed = 30) => {
  return new Promise(resolve => {
    const text = element.textContent
    let i = text.length
    
    const deleteChar = () => {
      if (i > 0) {
        element.textContent = text.substring(0, i - 1)
        i--
        setTimeout(deleteChar, speed)
      } else {
        resolve()
      }
    }
    
    deleteChar()
  })
}
```

### 4.12 动画暂停与恢复（Pause/Resume）

```javascript
// 点击暂停/恢复动画
let isPlaying = true

card.addEventListener('click', () => {
  if (isPlaying) {
    gsap.globalTimeline.pause()
    // 或暂停单个动画
    // cardAnimation.pause()
  } else {
    gsap.globalTimeline.resume()
    // cardAnimation.resume()
  }
  isPlaying = !isPlaying
})

// 自动播放/暂停
ScrollTrigger.create({
  trigger: '.autoplay-section',
  start: 'top center',
  onEnter: () => gsap.globalTimeline.play(),
  onLeave: () => gsap.globalTimeline.pause(),
  onEnterBack: () => gsap.globalTimeline.play(),
  onLeaveBack: () => gsap.globalTimeline.pause()
})
```

### 4.13 动画回调与钩子（Callbacks）

```javascript
// 动画完成回调
gsap.to(element, {
  scale: 1.5,
  duration: 1,
  ease: 'power2.out',
  onComplete: () => {
    console.log('动画完成')
  },
  onStart: () => {
    console.log('动画开始')
  },
  onUpdate: () => {
    console.log('动画进行中')
  }
})

// 带参数的回调
gsap.to(element, {
  x: 100,
  onComplete: completeHandler,
  onCompleteParams: [element, 'param2']
})

// Promise风格
const animateElement = (el) => {
  return new Promise(resolve => {
    gsap.to(el, {
      opacity: 1,
      duration: 0.5,
      onComplete: resolve
    })
  })
}

// 使用async/await
async function runAnimations() {
  await animateElement('.el1')
  await animateElement('.el2')
  await animateElement('.el3')
}
```

### 4.14 响应式动画（Responsive Animation）

```javascript
// 根据视口大小调整动画
const mm = gsap.matchMedia()

mm.add('(max-width: 768px)', () => {
  // 手机端动画
  gsap.to('.card', {
    y: 0,
    duration: 0.5
  })
  
  return () => {
    // 清理手机端动画
    gsap.killTweensOf('.card')
  }
})

mm.add('(min-width: 769px)', () => {
  // 桌面端动画
  gsap.to('.card', {
    y: -50,
    scale: 1.05,
    duration: 1
  })
  
  return () => {
    gsap.killTweensOf('.card')
  }
})
```

### 4.15 动画参数速查表

```javascript
// 常用缓动函数速查
const easingFunctions = {
  // 减速
  'power1.out': '快速启动，缓慢停止',
  'power2.out': '更快的减速',
  'power3.out': '极快的减速',
  'power4.out': '极速减速',
  
  // 加速
  'power1.in': '缓慢启动，快速停止',
  'power2.in': '更快的加速',
  'power3.in': '极快的加速',
  
  // 混合
  'power1.inOut': '缓慢启动和停止',
  'power2.inOut': '更快的混合',
  'power3.inOut': '极快的混合',
  
  // 弹性
  'back.out(1.7)': '向前冲后回弹',
  'back.inOut(1.7)': '双向回弹',
  'elastic.out(1, 0.3)': '强烈弹性',
  'bounce.out': '物理弹跳',
  
  // 平滑
  'sine.out': '平滑正弦减速',
  'sine.inOut': '平滑正弦往复',
  'circ.out': '圆形轨迹减速',
  'expo.out': '指数级减速',
  
  // 步进
  'steps(5)': '分5步完成',
  'none': '线性，无缓动'
}
```

---

## 五、动画参数速查表

### 5.1 缓动函数选择

| 缓动函数 | 效果描述 | 适用场景 |
|----------|----------|----------|
| `power1.out` | 平滑减速 | 数字递增、进度条 |
| `power2.out` | 中等减速 | 通用过渡 |
| `power3.out` | 快速减速 | 内容入场 |
| `back.out(1.7)` | 弹性回弹 | 卡片浮动、弹跳 |
| `elastic.out(1, 0.3)` | 强烈弹性 | 强调动画 |
| `bounce.out` | 物理弹跳 | 游戏元素 |
| `sine.inOut` | 平滑起伏 | 呼吸动画、脉冲 |
| `circ.inOut` | 缓入缓出 | 圆形轨迹 |

### 5.2 ScrollTrigger配置

```javascript
// 标准配置
scrollTrigger: {
  trigger: element,
  start: 'top 80%',      // 元素顶部到达视口80%时开始
  end: 'top 20%',        // 元素顶部到达视口20%时结束
  scrub: 1,               // 1秒跟随滚动（数值越大越慢）
  toggleActions: 'play none none reverse'  // 进入/离开/返回/回退
}

// 吸附配置
snap: 1 / (sections.length - 1)  // 面板吸附

// pin固定
pin: true,           // 固定触发元素
anticipatePin: 1     // 预判PIN
```

### 5.3 动画时长参考

| 动画类型 | 推荐时长 | 示例 |
|----------|----------|------|
| 微交互 | 0.2-0.4s | 悬停、点击 |
| UI过渡 | 0.3-0.6s | 显示/隐藏 |
| 入场动画 | 0.6-1.2s | 卡片入场 |
| 复杂动画 | 1-2s | 数字递增 |
| 持续动画 | 2-4s | 脉冲、旋转 |

---

## 六、组件动画组合

### 6.1 萌宠卡片完整动画

```javascript
// 萌宠卡片入场
gsap.set(card, { 
  x: direction.x, 
  y: direction.y, 
  opacity: 0, 
  rotation: -20,
  scale: 0.8
})

gsap.to(card, {
  x: 0,
  y: 0,
  opacity: 1,
  rotation: 0,
  scale: 1,
  duration: 1.2,
  ease: 'power3.out',
  scrollTrigger: {
    trigger: card,
    start: 'top 90%',
    toggleActions: 'play none none reverse'
  }
})

// 头像旋转入场
gsap.to(avatar, {
  rotation: 360,
  scale: 1,
  duration: 1.5,
  ease: 'back.out(1.7)',
  delay: 0.3
})

// 图片缩放视差
gsap.fromTo(img,
  { scale: 1.2 },
  {
    scale: 1,
    scrollTrigger: {
      trigger: card,
      start: 'top 80%',
      end: 'bottom 20%',
      scrub: 1.5
    }
  }
)

// 悬停效果
card.addEventListener('mouseenter', () => {
  gsap.to(card, { y: -15, scale: 1.03, duration: 0.4, ease: 'power2.out' })
  gsap.to(avatar, { rotation: '+=360', scale: 1.2, duration: 0.6, ease: 'back.out(1.7)' })
  gsap.to(border, { borderColor: 'rgba(255,107,129,0.8)', boxShadow: '0 0 30px rgba(255,107,129,0.5)' })
})

card.addEventListener('mouseleave', () => {
  gsap.to(card, { y: 0, scale: 1, duration: 0.4 })
  gsap.to(avatar, { scale: 1, duration: 0.3 })
  gsap.to(border, { borderColor: 'rgba(255,255,255,0.2)', boxShadow: 'none' })
})
```

### 6.2 统计卡片动画

```javascript
// 卡片入场
gsap.from(statCard, {
  y: 100,
  scale: 0.5,
  opacity: 0,
  duration: 0.8,
  ease: 'back.out(1.7)',
  stagger: 0.1,
  scrollTrigger: {
    trigger: '.stats-section',
    start: 'top 80%'
  }
})

// 图标脉冲动画
gsap.to(icon, {
  scale: [1, 1.1, 1],
  duration: 2,
  repeat: -1,
  ease: 'sine.inOut'
})

// 数字递增
gsap.to(number, {
  textContent: target,
  duration: 2,
  ease: 'power1.out',
  snap: { textContent: 1 },
  scrollTrigger: {
    trigger: '.stats-section',
    start: 'top 70%'
  },
  onUpdate: function() {
    number.textContent = Math.round(this.targets()[0].textContent)
  }
})

// 标签依次入场
gsap.from(tag, {
  y: 20,
  opacity: 0,
  duration: 0.5,
  stagger: 0.1,
  delay: 0.5,
  ease: 'power2.out'
})
```

---

## 七、横向滚动面板

### 7.1 基础横向滚动

```javascript
const initHorizontalScroll = () => {
  const sections = gsap.utils.toArray('.scroll-panel')
  const track = document.querySelector('.horizontal-scroll-track')

  // 主横向滚动动画
  const horizontalAnimation = gsap.to(sections, {
    xPercent: -100 * (sections.length - 1),
    ease: 'none',
    scrollTrigger: {
      trigger: '.horizontal-scroll-section',
      pin: true,
      scrub: 1,
      anticipatePin: 1,
      snap: 1 / (sections.length - 1),
      start: 'top top',
      end: () => '+=' + (track.offsetWidth - window.innerWidth)
    }
  })

  return horizontalAnimation
}
```

### 7.2 面板内卡片动画（使用containerAnimation）

```javascript
// 面板1：萌宠相册 - 水墨晕染揭示
sections.forEach((section, index) => {
  if (index === 0) {
    // 水墨滴落效果
    gsap.to('.ink-drop', {
      scale: [0, 2, 5, 10],
      opacity: [0.8, 0.9, 0.7, 0],
      scrollTrigger: {
        trigger: section,
        start: 'left 80%',
        end: 'left 20%',
        containerAnimation: horizontalAnimation,
        scrub: 2
      }
    })

    // 卡片入场
    gsap.fromTo('.gallery-item',
      { y: 100, scale: 0.8, rotationY: 20, filter: 'blur(10px)' },
      {
        y: 0,
        scale: 1,
        rotationY: 0,
        filter: 'blur(0px)',
        stagger: 0.15,
        ease: 'back.out(1.7)',
        scrollTrigger: {
          trigger: section,
          start: 'left 70%',
          end: 'left 10%',
          containerAnimation: horizontalAnimation,
          scrub: 1
        }
      }
    )
  }
})
```

---

## 八、背景动画效果

### 8.1 粒子背景

```javascript
// 粒子类
class Particle {
  constructor() {
    this.x = Math.random() * canvas.width
    this.y = Math.random() * canvas.height
    this.size = Math.random() * 20 + 10
    this.speedX = (Math.random() - 0.5) * 1.5
    this.speedY = (Math.random() - 0.5) * 1.5
    this.opacity = Math.random() * 0.5 + 0.2
  }

  update() {
    this.x += this.speedX
    this.y += this.speedY
    // 边界反弹
    if (this.x < 0 || this.x > canvas.width) this.speedX *= -1
    if (this.y < 0 || this.y > canvas.height) this.speedY *= -1
  }

  draw() {
    ctx.globalAlpha = this.opacity
    ctx.fillText('❤', this.x, this.y)  // 爱心粒子
  }
}

// 连接附近粒子
particles.forEach((p1, i) => {
  particles.slice(i + 1).forEach(p2 => {
    const dx = p1.x - p2.x
    const dy = p1.y - p2.y
    const distance = Math.sqrt(dx * dx + dy * dy)
    
    if (distance < 200) {
      ctx.beginPath()
      ctx.moveTo(p1.x, p1.y)
      ctx.lineTo(p2.x, p2.y)
      ctx.strokeStyle = `rgba(255,255,255,${0.3 - distance/200*0.3})`
      ctx.stroke()
    }
  })
})
```

### 8.2 渐变动画背景

```javascript
// 多层渐变背景动画
const glassLayers = gsap.utils.toArray('.glass-layer')

glassLayers.forEach((layer, i) => {
  gsap.to(layer, {
    backgroundPosition: '200% 200%',
    duration: 10 + i * 2,
    repeat: -1,
    ease: 'none'
  })
})

// 背景脉冲
gsap.to('.bg-gradient', {
  backgroundPosition: '50% 50%',
  duration: 8,
  repeat: -1,
  yoyo: true,
  ease: 'sine.inOut'
})
```

---

## 九、响应式断点

```css
/* 平板 (1024px) */
@media (max-width: 1024px) {
  .pets-content,
  .services-grid,
  .gallery-grid { grid-template-columns: repeat(2, 1fr); }
  
  .stories-container { width: 600px; height: 600px; }
  
  .scroll-panel { padding: 60px 40px; }
}

/* 手机 (768px) */
@media (max-width: 768px) {
  .scroll-panel { padding: 40px 20px; }
  
  .pets-content,
  .services-grid,
  .gallery-grid { grid-template-columns: 1fr; }
  
  .stories-container { width: 400px; height: 400px; }
  
  .nav-links { display: none; }
  
  /* 简化动画 */
  .card-class { 
    transform: none !important;
    transition: all 0.2s ease;
  }
}
```

---

## 十、萌宠主题emoji参考

```
🐕 狗    🐶 狗狗   🐱 猫    🐈 猫咪   🐰 兔子   🐹 仓鼠
🦜 鹦鹉   🐦 鸟    🦮 导盲犬  🐕‍🦺 服务犬  🐩 贵宾犬
🐕‍🦺 服务犬 🐾 爪印   🐇 兔子   🦮 导盲犬
```

---

## 十一、代码片段库

### 11.1 带动画的导航栏

```html
<nav class="navbar" id="navbar">
  <div class="navbar-content">
    <div class="logo">
      <i class="fas fa-paw"></i>
      <span>萌宠星球</span>
    </div>
    <div class="nav-links">
      <a href="#section" class="nav-link">链接</a>
    </div>
  </div>
  <!-- 滚动进度条 -->
  <div class="nav-progress"></div>
</nav>
```

```css
.navbar {
  transition: all 0.3s ease;
}

.navbar.scrolled {
  padding: 10px 50px;
  box-shadow: 0 5px 30px rgba(255, 107, 129, 0.3);
}

.nav-progress {
  position: absolute;
  bottom: 0;
  left: 0;
  height: 3px;
  background: linear-gradient(90deg, #ff6b81, #ff4757);
  width: 0;
  transition: width 0.1s linear;
}
```

```javascript
// 滚动进度
window.addEventListener('scroll', () => {
  const scrollTop = window.scrollY
  const docHeight = document.documentElement.scrollHeight - window.innerHeight
  const progress = (scrollTop / docHeight) * 100
  document.querySelector('.nav-progress').style.width = progress + '%'
})
```

### 11.2 Hero区域（带打字效果）

```html
<section class="hero-section" id="hero">
  <canvas id="hero-canvas"></canvas>
  <div class="hero-content">
    <h1 class="hero-title">
      <span class="typing-text"></span>
      <span class="cursor">|</span>
    </h1>
    <p class="hero-subtitle">副标题</p>
    <a href="#" class="hero-cta">按钮</a>
    <div class="hero-tags">
      <span class="hero-tag">标签</span>
    </div>
  </div>
  <div class="scroll-indicator">
    <span>向下滚动</span>
    <div class="arrow">↓</div>
  </div>
</section>
```

```javascript
// 打字效果
const typingEffect = () => {
  const text = '萌宠星球'
  const element = document.querySelector('.typing-text')
  let index = 0
  
  setInterval(() => {
    element.textContent = text.substring(0, index + 1)
    index = (index + 1) % (text.length + 1)
    if (index === 0) setTimeout(() => {}, 1000)
  }, 200)
}

// 滚动指示器动画
gsap.to('.scroll-indicator .arrow', {
  y: 20,
  duration: 1,
  repeat: -1,
  yoyo: true,
  ease: 'sine.inOut'
})
```

### 11.3 联系表单（带验证动画）

```html
<form class="contact-form" onsubmit="event.preventDefault();">
  <div class="form-group">
    <input type="text" required placeholder=" ">
    <label>姓名</label>
    <div class="input-line"></div>
  </div>
  <button type="submit" class="submit-btn">
    <span>发送</span>
    <div class="btn-progress"></div>
  </button>
</form>
```

```css
.form-group {
  position: relative;
}

.form-group input {
  width: 100%;
  padding: 15px 0;
  border: none;
  border-bottom: 2px solid #ddd;
  background: transparent;
  transition: border-color 0.3s;
}

.form-group input:focus {
  border-color: #ff6b81;
}

.form-group label {
  position: absolute;
  left: 0;
  top: 15px;
  color: #999;
  transition: all 0.3s;
  pointer-events: none;
}

.form-group input:focus + label,
.form-group input:not(:placeholder-shown) + label {
  top: -10px;
  font-size: 12px;
  color: #ff6b81;
}
```

---

## 十二、性能优化建议

### 12.1 动画性能

```javascript
// ✅ 使用 will-change
element.style.willChange = 'transform, opacity'

// ✅ 使用 gsap.set 预设
gsap.set(element, { x: 0, y: 0 })

// ✅ 及时清理
onUnmounted(() => {
  ScrollTrigger.getAll().forEach(st => st.kill())
  ctx?.revert()
})

// ✅ 使用 transform 而非 top/left
gsap.to(element, { x: 100, y: 50 })  // ✅
gsap.to(element, { left: 100, top: 50 })  // ❌
```

### 12.2 避免布局抖动

```css
/* ✅ 使用 transform 和 opacity */
.element {
  transform: translateZ(0);
  opacity: 1 !important;
}

/* ✅ 使用 GPU 加速 */
.gpu-accelerated {
  transform: translate3d(0, 0, 0);
  backface-visibility: hidden;
}
```

---

*文档版本: 3.0 | 更新日期: 2026-04-23*

**主要更新（v3.0）：**
- 新增15种高级GSAP动画技巧（共27种）
- 新增时间线动画（Timeline）、SVG路径绘制、文字拆解动画
- 新增3D透视卡片、遮罩揭示、序列入场动画
- 新增拖拽交互、SVG图形变形、粒子动画系统
- 新增滚动同步动画、复杂过渡动画、打字机效果
- 新增动画暂停/恢复、回调钩子、响应式动画
- 新增完整的缓动函数速查表（15种）
- 新增GSAP.context最佳实践、性能优化建议

**v2.0更新：**
- 新增12种高级GSAP动画技巧
- 新增水墨晕染、3D翻转、毛玻璃等特效
- 新增完整的动画参数速查表
- 新增鼠标跟随、视差滚动等交互
